# Streamlit secrets (never commit real passwords)
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Local QC output (regenerated by qc_runner.py)
QC Results/

# Large source data lives outside the repo; only the prepared parquet ships.
*.csv.gz

# Local-only publish helper (reads the GitHub token; must never be committed)
publish.py
*.command
