# Copy this file to secrets.toml (local) or paste into the Streamlit Community
# Cloud "Secrets" box for the deployed app. secrets.toml is gitignored.
#
# If APP_PASSWORD is not set, the app falls back to the built-in default.
APP_PASSWORD = "change-me"
